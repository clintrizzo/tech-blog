# tech-blog

# installation
npm init
npm install

# usage
run following command in terminal "mysql -u root -p" 
then answer with your password

after password:
source db/schema.sql
quit
npm run seed
npm start

